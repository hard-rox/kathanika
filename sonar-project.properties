# must be unique in a given SonarQube instance
sonar.projectKey=kathanika

# --- optional properties ---

# defaults to project key
#sonar.projectName=kathanika
# defaults to 'not provided'
#sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Defaults to .
#sonar.sources=.
 
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

#Local sonarcloud
#sudo docker run --rm --network=host -e SONAR_HOST_URL="http://127.0.0.1:9001" -e SONAR_LOGIN="sqp_664c1ff5056751afef109a85a3a073bd46c22686" -v "/home/rox/code-projects/kathanika:/usr/src" sonarsource/sonar-scanner-cli